spring:
  datasource:
    url: jdbc:mysql://localhost:3307/project_pulse?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: secret

  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    edu.tcu.cs.projectpulse: DEBUG
    org.springframework.security: DEBUG
